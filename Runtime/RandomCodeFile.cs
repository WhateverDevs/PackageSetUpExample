using UnityEngine;

namespace WhateverDevs.PackageSetUpExample.Runtime
{
    public class RandomCodeFile : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
